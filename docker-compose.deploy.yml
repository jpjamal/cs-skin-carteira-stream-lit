services:
  cs2-tracker:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: cs2-skin-tracker
    volumes:
      - ./data:/app/data
    command: streamlit run app.py --server.baseUrlPath /${APP_NAME}
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
    networks:
      - traefik_proxy
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=traefik_proxy"
      - "traefik.http.routers.cs2tracker.rule=PathPrefix(`/${APP_NAME}`)"
      - "traefik.http.routers.cs2tracker.entrypoints=web"
      - "traefik.http.services.cs2tracker.loadbalancer.server.port=8501"

networks:
  traefik_proxy:
    external: true

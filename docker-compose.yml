services:
  cs2-tracker:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: cs2-skin-tracker
    ports:
      - "8501:8501"
    volumes:
      - .:/app
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
